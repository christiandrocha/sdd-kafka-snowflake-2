# Local quality gate — runs before every `git commit`.
#
# Install:
#   pip install pre-commit
#   pre-commit install
#
# Run over everything once:
#   pre-commit run --all-files
#
# `detect-private-key` is the hook that matters most here: this project handles
# a Snowflake RSA key pair, and the key has been leaked out of `.env` once
# already (2026-08-11).

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.9
    hooks:
      - id: ruff
        args: ["--fix", "--output-format=full"]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        exclude: ^dbt/(dbt_packages|target)/
      - id: check-toml
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=1500"]
      - id: detect-private-key

  - repo: https://github.com/adrienverge/yamllint
    rev: v1.35.1
    hooks:
      - id: yamllint
        files: ^(connectors|observability)/.*\.ya?ml$

  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.2
    hooks:
      - id: bandit
        args: ["-ll", "--skip", "B101,B608"]
        types: [python]
